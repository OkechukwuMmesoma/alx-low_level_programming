#include "main.h"

/**
 * _abs - will compute the absolute value of an integer
 * @n: and the int to check
 * Return: The absolute value of it
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
